-- 판매자 특성 (개인 메모용) 컬럼 추가
-- 변경 사항:
--   (신규) 판매자특성_메모_선택  JSONB DEFAULT '[]'::jsonb
--          ← 설정의 '판매자 특성 (개인 메모용)'에서 체크된 항목 배열. 01~05 미참조.
--
-- 기존 '판매자특성_선택'은 '판매자 특성 (파이프라인 활용형)' 용도로 의미 재정의.

ALTER TABLE 상품
  ADD COLUMN IF NOT EXISTS 판매자특성_메모_선택 JSONB DEFAULT '[]'::jsonb;
