.env
credentials/
db/mgoon.sqlite
db/*.sqlite-journal
__pycache__/
*.pyc
.venv/
.streamlit/secrets.toml

# 토큰 파일 (Google OAuth 등)
token.pickle
*.pickle

# 평문 계정정보
계정정보(API키-DB정보 등).txt

# 부수 자료 (학습용 강의·테스트 데이터·수동 백업)
강의 원본/
테스트용 제품 정보/
비전패스 프롬프트 백업.txt

# 로컬 .py/.md 스냅샷 백업 (git 외 belt-and-suspenders)
_backups/

# 테스트 데이터 (이미지·영상 등 바이너리)
테스트/

# DB 디렉토리의 임시 출력·덤프 (Supabase OpenAPI dump, psql 출력 등)
db/out.txt
db/*.dump.txt
db/*.tmp
db/*.log

# 일반 임시·로그 파일
*.tmp
*.log
