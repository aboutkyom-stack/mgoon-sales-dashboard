-- ============================================================
-- 상품 테이블의 구_카탈로그_id, 구_v2_id 컬럼 제거
-- 목적: 동료 DB 1회성 이관용 컬럼이었고 어떤 페이지/스크립트도 읽지 않음
-- 실행: db/run_drop_구_id_컬럼.py (멱등 — 재실행 안전)
-- ============================================================

ALTER TABLE 상품 DROP COLUMN IF EXISTS 구_카탈로그_id;
ALTER TABLE 상품 DROP COLUMN IF EXISTS 구_v2_id;
