-- ============================================================
-- 엠군_네이밍 테이블에 분류 컬럼 추가
-- 목적: 제품명 / 브랜드명 / (추후 오픈마켓 상품명 등) 분류별 결과 구분
-- 멱등 (IF NOT EXISTS)
-- 실행: db/run_add_네이밍_분류.py
-- ============================================================

ALTER TABLE 엠군_네이밍
ADD COLUMN IF NOT EXISTS 분류 TEXT;

CREATE INDEX IF NOT EXISTS idx_네이밍_타겟_분류
ON 엠군_네이밍(타겟_id, 분류);
