-- ============================================================
-- 상품_파일.상품_id 컬럼 NOT NULL 제약 제거
-- 목적: 이미지-상품 매칭 페이지에서 "선택 안 함" 저장 시 NULL 저장 가능하게.
--       이미 페이지 코드는 is_('상품_id', 'null')로 미매칭을 표현하고 있어
--       schema와 코드 의도가 일치하도록 정합.
-- 멱등 (DROP NOT NULL은 이미 NULLABLE이면 no-op).
-- ============================================================

ALTER TABLE 상품_파일 ALTER COLUMN 상품_id DROP NOT NULL;
